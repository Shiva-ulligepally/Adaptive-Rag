{
  "name": "adaptive-rag",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
